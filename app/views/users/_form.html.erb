<%= form_with(model: user) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this usuario from being saved:</h2>

      <ul>
        <% user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :login %>
    <%= form.text_field :login %>
  </div>

  <div class="field">
    <%= form.label :password %> (deixe vazio para não alterar) <br />
    <%= form.password_field :password, value: '' %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.select('active', options_for_select([[1,1], [0,0]], @user.active)) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
